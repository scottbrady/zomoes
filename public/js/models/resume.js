define(
[
	'backbone'
],
function(
	Backbone
) {
	var ResumeModel = Backbone.Model.extend({

		urlRoot : '/api/resume'

		//////////////////////////////////////////////////////////////////////
		// Public methods ///////////////////////////////////////////////////
		////////////////////////////////////////////////////////////////////

	});

	return ResumeModel;
});